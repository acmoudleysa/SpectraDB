[pytest]
# Minimum version of pytest required
minversion = 6.0

# Additional command-line options
addopts = --maxfail=5 --disable-warnings -q

# Directories to search for test files
testpaths =
    tests

# Patterns to match test files and test functions
python_files = test_*.py
python_classes = *Tests
python_functions = test_*
